import 'package:get/get.dart';

class CheckEmailBinding extends Bindings {
  @override
  void dependencies() {
    // Controller is put in widget when view is created because it needs email arg
  }
}
