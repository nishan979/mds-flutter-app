import 'package:flutter/material.dart';

class AppColors {
  // Primary Colors
  static const Color primary = Color(0xFF6200EE);
  static const Color primaryVariant = Color(0xFF3700B3);
  static const Color secondary = Color(0xFFD4AF37); // Golden accent
  static const Color secondaryVariant = Color(0xFFB8860B);

  // Background Colors - Dark theme from login image
  static const Color backgroundDark = Color(0xFF0A1628); // Deep navy blue

  static const Color backgroundGradientStart = Color(0xFF1A2744);
  static const Color backgroundGradientEnd = Color(0xFF0A0E1A);

  // ------------nishan----------------
    static const Color backgroundChikichiki = Color(0xFF02060e);

  // Standard backgrounds
  static const Color background = Color(0xFFF5F5F5);
  static const Color surface = Color(0xFFFFFFFF);
  static const Color error = Color(0xFFB00020);

  // Text Colors
  static const Color textPrimary = Color(0xFF000000);
  static const Color textSecondary = Color(0xFF757575);
  static const Color textHint = Color(0xFFBDBDBD);
  static const Color textWhite = Color(0xFFFFFFFF);
  static const Color textGolden = Color(
    0xFFD4AF37,
  ); // Golden text for dark backgrounds

  // Other Common Colors
  static const Color white = Color(0xFFFFFFFF);
  static const Color black = Color(0xFF000000);
  static const Color grey = Color(0xFF9E9E9E);
  static const Color greyLight = Color(0xFFE0E0E0);
  static const Color greyDark = Color(0xFF616161);
  static const Color transparent = Colors.transparent;

  // Success, Warning, Info
  static const Color success = Color(0xFF4CAF50);
  static const Color warning = Color(0xFFFFC107);
  static const Color info = Color(0xFF2196F3);

  // Login page specific
  static const Color loginOverlay = Color(
    0xCC0A1628,
  ); // Semi-transparent overlay
}
